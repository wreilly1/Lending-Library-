Team Member Name:
William Reilly
Kevin Delima
Jonel Pouertiet-Santana
Jason Yang
Team Member B-Numbers:	        
B00958441
B00915951
B00818880
B00891109
One Email Address:		
wreilly1@binghamton.edu
Add text here as needed to document the status of your project.

Commands list:

DOM Manipulation:

document.querySelector(selector) - Finds the first element matching the CSS selector.
document.getElementById(id) - Finds an element by its ID.
makeElement(tag, attributes?, content?) - Creates a DOM element (assumed custom utility).
element.append(children...) - Adds child elements to the parent element.
element.addEventListener(event, handler) - Attaches an event listener to an element.
element.innerHTML - Sets or clears the HTML content of an element.
element.textContent - Sets or clears the text content of an element.

Error Handling:

displayErrors(errorsArray) - Displays a list of error messages (assumed custom utility).
Errors.error(message, options) - Creates a standardized error object.
this.clearErrors() - Clears all displayed errors (assumed custom method).

Web Service Requests:

this.ws.findBooksByUrl(url) - Fetches books from the server using a URL.
this.ws.getBookByUrl(url) - Fetches book details from the server.
this.ws.checkoutBook({ isbn, patronId }) - Processes book checkout for a patron.
this.ws.getLends(isbn) - Fetches the list of borrowers for a book.
this.ws.returnBook({ isbn, patronId }) - Processes returning a book.


Data Validation and Processing:

event.target.value.trim() - Gets and trims whitespace from input values.
result.isOk - Checks if the server response is successful.

URL and Navigation:

new URL(baseUrl) - Creates a new URL object from a string.
url.searchParams.set(key, value) - Sets query parameters in the URL.
links.next.href - Retrieves the URL for the next page (assumes pagination links).

Logging and Debugging:

console.log(message) - Outputs messages to the browser console.
console.error(message) - Outputs error messages to the browser console.



